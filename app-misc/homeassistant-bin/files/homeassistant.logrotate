# logrotate script for homeassistant

/var/log/homeassistant/*.log {
        daily
        missingok
        copytruncate
        rotate 9
        compress
        notifempty
        su root root
        olddir /var/log/homeassistant/old
}

